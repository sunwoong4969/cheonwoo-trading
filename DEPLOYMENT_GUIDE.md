# 배포 가이드

## 🏗️ 아키텍처 구조

```
┌─────────────────┐         ┌─────────────────┐         ┌─────────────────┐
│   프론트엔드     │  ────>  │   백엔드 API     │  ────>  │   MariaDB        │
│  GitHub Pages   │  HTTP   │  (별도 호스팅)   │  SQL    │   (데이터베이스)  │
│  (정적 파일)     │         │  Node.js/Express│         │                  │
└─────────────────┘         └─────────────────┘         └─────────────────┘
```

**중요:** GitHub Pages는 정적 파일만 호스팅하므로 Node.js 서버를 실행할 수 없습니다.
따라서 백엔드는 별도로 호스팅해야 합니다.

---

## 🔄 GitHub ↔ Railway 연동 흐름 (자동 배포)

### 핵심 개념: 코드 변경 → GitHub 푸시 → Railway 자동 배포

```
┌─────────────────────────────────────────────────────────────────┐
│  1. 로컬에서 코드 수정 (예: server.js에 Twilio 기능 추가)        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. Git 커밋 & 푸시                                             │
│     git add .                                                  │
│     git commit -m "Add Twilio SMS feature"                     │
│     git push origin main                                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  3. GitHub 저장소에 코드 업로드                                 │
│     (GitHub.com에서 변경사항 확인 가능)                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  4. Railway가 자동으로 감지                                     │
│     (Railway는 GitHub와 연동되어 있음)                          │
│     → "새로운 커밋이 있네? 배포 시작!"                          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  5. Railway가 자동으로 배포                                     │
│     - GitHub에서 최신 코드 다운로드                             │
│     - npm install (의존성 설치)                                │
│     - node server.js (서버 실행)                                │
│     - 배포 완료!                                                │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  6. 프로덕션 서버에 새 기능 적용됨                               │
│     (예: https://cheonwoo-api.railway.app)                      │
│     → 이제 실제 서버에서도 Twilio 기능 사용 가능!               │
└─────────────────────────────────────────────────────────────────┘
```

### ⚠️ 중요한 점

1. **GitHub에 올리지 않으면?**
   - ❌ Railway는 변경사항을 모름
   - ❌ 프로덕션 서버에는 이전 버전 그대로
   - ✅ 로컬에서는 새 기능 작동 (로컬 서버 실행 시)

2. **GitHub에 올리면?**
   - ✅ Railway가 자동으로 감지
   - ✅ 자동으로 배포 시작
   - ✅ 프로덕션 서버에 새 기능 적용

3. **환경 변수는?**
   - Railway 대시보드에서 직접 설정
   - GitHub에 올리지 않아도 됨 (보안상 안전)
   - `.env` 파일은 절대 GitHub에 올리지 말 것!

---

## 📋 배포 단계

### 1단계: 백엔드 서버 배포

백엔드 서버를 호스팅할 플랫폼을 선택하세요:

#### 옵션 1: Railway (권장 - 무료 플랜 제공)
- **사이트:** https://railway.app
- **장점:** 무료 플랜, 자동 배포, 쉬운 설정
- **단점:** 무료 플랜은 일정 시간 후 슬립 모드

#### 옵션 2: Render
- **사이트:** https://render.com
- **장점:** 무료 플랜, 쉬운 설정
- **단점:** 무료 플랜은 슬립 모드

#### 옵션 3: Heroku
- **사이트:** https://heroku.com
- **장점:** 널리 사용됨
- **단점:** 무료 플랜 종료됨 (유료)

#### 옵션 4: Vercel (Serverless Functions)
- **사이트:** https://vercel.com
- **장점:** 무료, 빠른 배포
- **단점:** Serverless 구조로 약간의 수정 필요

---

### 2단계: 백엔드 배포 (Railway 예시)

#### 2-1. Railway 프로젝트 생성

1. **Railway 계정 생성**
   - https://railway.app 접속
   - GitHub로 로그인

2. **새 프로젝트 생성**
   - "New Project" 클릭
   - "Deploy from GitHub repo" 선택
   - 저장소 선택

3. **환경 변수 설정**
   - 프로젝트 > Variables 탭
   - 다음 변수 추가:
     ```
     PORT=3000
     DB_HOST=여기에_MariaDB_서버_주소
     DB_USER=여기에_MariaDB_사용자명
     DB_PASSWORD=여기에_MariaDB_비밀번호
     DB_NAME=cheonwoo_trading
     DB_PORT=3306
     ```

4. **배포 확인**
   - 배포 완료 후 "Settings" > "Domains"에서 URL 확인
   - 예: `https://cheonwoo-api.railway.app`

---

### 3단계: 프론트엔드 API URL 설정

백엔드 서버 URL을 받은 후:

1. **`script.js` 파일 수정**
   - 60번 줄 근처의 `API_BASE_URL` 수정:
   ```javascript
   const API_BASE_URL = isLocalhost 
       ? 'http://localhost:3000' 
       : 'https://cheonwoo-api.railway.app'; // 실제 백엔드 URL로 변경
   ```

2. **GitHub에 커밋 및 푸시**
   ```bash
   git add script.js
   git commit -m "Update API endpoint for production"
   git push origin main
   ```

---

### 4단계: MariaDB 설정

#### 옵션 1: 로컬 MariaDB (개발용)
- 로컬에 MariaDB 설치
- `database.sql` 실행
- `.env` 파일에 로컬 DB 정보 입력

#### 옵션 2: 클라우드 MariaDB (프로덕션용)

**Railway MariaDB:**
1. Railway 프로젝트에서 "New" > "Database" > "Add MariaDB"
2. 자동으로 환경 변수 생성됨
3. `database.sql` 스크립트 실행

**다른 옵션:**
- **PlanetScale:** https://planetscale.com (MySQL 호환)
- **Aiven:** https://aiven.io
- **AWS RDS:** https://aws.amazon.com/rds

---

### 5단계: CORS 설정 확인

`server.js`의 CORS 설정이 GitHub Pages 도메인을 포함하는지 확인:

```javascript
const corsOptions = {
    origin: [
        'https://sunwoong4969.github.io',  // GitHub Pages 도메인
        'http://localhost:8080',            // 로컬 개발
        'http://127.0.0.1:8080'            // 로컬 개발
    ],
    // ...
};
```

**GitHub Pages URL이 다르다면:**
- `server.js`의 `origin` 배열에 실제 GitHub Pages URL 추가

---

## 🧪 테스트

### 로컬 테스트

1. **백엔드 서버 실행:**
   ```bash
   npm start
   ```

2. **프론트엔드 실행:**
   ```bash
   python -m http.server 8080
   ```

3. **브라우저에서 테스트:**
   - http://localhost:8080 접속
   - 문의하기 폼 작성 및 제출
   - 콘솔에서 API 호출 확인

### 프로덕션 테스트

1. **GitHub Pages에서 테스트:**
   - https://sunwoong4969.github.io/cheonwoo-trading/ 접속
   - 문의하기 폼 작성 및 제출
   - 브라우저 개발자 도구 > Network 탭에서 API 호출 확인

2. **백엔드 로그 확인:**
   - Railway/Render 대시보드에서 로그 확인
   - 문의 데이터가 DB에 저장되었는지 확인

---

## 🔧 문제 해결

### CORS 오류

**증상:** 브라우저 콘솔에 "CORS policy" 오류

**해결:**
1. `server.js`의 `corsOptions.origin`에 프론트엔드 URL이 포함되어 있는지 확인
2. 백엔드 서버 재시작

### API 연결 실패

**증상:** "서버 연결 오류가 발생했습니다"

**해결:**
1. 백엔드 서버가 실행 중인지 확인
2. `script.js`의 `API_BASE_URL`이 올바른지 확인
3. 브라우저 개발자 도구 > Network 탭에서 실제 요청 URL 확인

### 데이터베이스 연결 실패

**증상:** 백엔드 로그에 "MariaDB 연결 실패"

**해결:**
1. 환경 변수가 올바르게 설정되었는지 확인
2. MariaDB 서버가 실행 중인지 확인
3. 방화벽 설정 확인 (클라우드 DB인 경우)

---

## 📝 체크리스트

### 백엔드 배포
- [ ] Railway/Render 계정 생성
- [ ] 프로젝트 생성 및 GitHub 연결
- [ ] 환경 변수 설정 (DB 정보)
- [ ] 배포 완료 및 URL 확인
- [ ] MariaDB 연결 확인

### 프론트엔드 배포
- [ ] `script.js`의 `API_BASE_URL` 수정
- [ ] GitHub에 커밋 및 푸시
- [ ] GitHub Pages 배포 확인

### 테스트
- [ ] 로컬 환경 테스트
- [ ] 프로덕션 환경 테스트
- [ ] 문의 폼 제출 테스트
- [ ] 데이터베이스 저장 확인

---

## 💡 추가 팁

### 환경 변수 관리

**로컬 개발:**
- `.env` 파일 사용 (Git에 커밋하지 않음)

**프로덕션:**
- Railway/Render 대시보드에서 환경 변수 설정
- 민감한 정보는 절대 코드에 하드코딩하지 않음

### 보안

1. **API 키 보호:**
   - 환경 변수로 관리
   - `.env` 파일을 `.gitignore`에 추가

2. **CORS 제한:**
   - 허용된 도메인만 명시적으로 추가
   - 와일드카드(`*`) 사용 지양

3. **입력 검증:**
   - 프론트엔드와 백엔드 모두에서 검증
   - SQL Injection 방지 (이미 `mysql2`의 prepared statement 사용)

---

## 🚀 빠른 시작 (Railway)

1. **Railway 계정 생성 및 로그인**

2. **프로젝트 생성:**
   ```
   New Project > Deploy from GitHub repo
   ```

3. **환경 변수 설정:**
   ```
   DB_HOST=...
   DB_USER=...
   DB_PASSWORD=...
   DB_NAME=cheonwoo_trading
   DB_PORT=3306
   ```

4. **배포 URL 확인:**
   ```
   Settings > Domains > Copy URL
   ```

5. **`script.js` 수정:**
   ```javascript
   const API_BASE_URL = isLocalhost 
       ? 'http://localhost:3000' 
       : 'https://your-railway-url.railway.app';
   ```

6. **GitHub에 푸시 및 배포 완료!**

